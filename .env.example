# Exemplo de variáveis de ambiente
# Copie este arquivo para .env e preencha com seus valores reais

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Database (se usar PostgreSQL local)
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d

# API Configuration
PORT=4000
NODE_ENV=development

# CORS Configuration (backend)
# Produção: defina para o domínio público do frontend (ex.: https://decolagem-gf-frontend.vercel.app)
# Staging/Preview: o backend já permite *.vercel.app do projeto via regra interna
CORS_ORIGIN=https://decolagem-gf-frontend.vercel.app
# URL pública do frontend (usado por redirects do backend, ex.: reset de senha)
FRONTEND_URL=https://decolagem-gf-frontend.vercel.app
# Endpoints de desenvolvimento (/dev/*): habilite somente em ambientes de desenvolvimento
# Para produção e preview/staging, mantenha como 'false'
ALLOW_DEV_ENDPOINTS=false

# Frontend URLs
# DEV: deixe VITE_API_URL vazio para usar o proxy do Vite (/api -> http://localhost:4000)
# STAGING/PROD: não é necessário definir VITE_API_URL (o frontend usa window.location.origin)
VITE_API_URL=
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Email Configuration (se usar)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# File Upload (se usar)
UPLOAD_MAX_SIZE=10485760
ALLOWED_FILE_TYPES=jpg,jpeg,png,pdf,doc,docx

# Redis (se usar)
REDIS_URL=redis://localhost:6379