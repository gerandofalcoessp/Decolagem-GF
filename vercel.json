{
  "version": 2,
  "buildCommand": "npm --prefix backend run vercel-build && rm -rf api/_backend_dist && mkdir -p api/_backend_dist && cp -R backend/dist/* api/_backend_dist/ && cp backend/dist/server.js api/server.js && mkdir -p public",
  "routes": [
    { "src": "/api/health", "dest": "/api?path=health" },
    { "src": "/health", "dest": "/api?path=health" },
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/[...path]" }
  ],
  "env": {
    "NODE_ENV": "production",
    "CORS_ORIGIN": "https://decolagem-gf-frontend.vercel.app",
    "FRONTEND_URL": "https://decolagem-gf-frontend.vercel.app"
  }
}